version: "3.7"

services:
  db:
    image: postgres:12.18-bullseye
    container_name: db
    ports:
      - 1221:5432
    env_file:
      - .env

  redis:
    image: redis:7
    container_name: redis_app
    command: --port 5370
    ports:
      - 5370